package main.jobs;

import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.Test;

import java.time.LocalDate;
import java.time.temporal.ChronoUnit;

import static org.junit.jupiter.api.Assertions.*;

class ProjectTest {
Project project;
    @BeforeAll
    public void setUp() {
//        project = Project.generateRandomProject();
    }

    @Test
    void generateDeadline() {
//        var now = LocalDate.now();
//        var deadline = Project.generateDeadline();
//
//        int time = 0;
//        var techStack = project.getTechStackAndWorkload();
//        var getValues = techStack.values();
//        for (Integer value : getValues) {
//            time = +value;
//        }
//
//        assertTrue(now.isBefore(deadline));
//        int timeLeft = (int) ChronoUnit.DAYS.between(deadline, now);
//        assertTrue(time > timeLeft);

    }

}